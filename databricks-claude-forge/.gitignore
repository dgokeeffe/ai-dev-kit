# Skills (copied from databricks-skills on startup)
skills/

# Projects directory (local development)
projects/

# Packages (copied from sibling dirs during legacy deployment)
packages/

# Conda environment (for Databricks Apps deployment)
conda-env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# Environment (app.yaml is tracked for git-based deployment)
.env.local

# Node (client + server-node)
client/node_modules/
server-node/node_modules/
# client/out/ is NOT tracked — the platform runs `npm run build` automatically
# during deployment (see package.json build script and deployment logic docs).
client/out/
client/dist/
client/.vite/

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Personal/debug files
progress.txt
test_*.js
screenshots/
*_daveok*
